service_account key can be downlaoded, once only from your  Google API Console ( https://console.developers.google.com/)


1° set up (or reuse) a project
2° enable PeopleAPI if isn't
3° setup Credentials => ServiceAccount
4° download service_account key.json should be proposed when settings OK
